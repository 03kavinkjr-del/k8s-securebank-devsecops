apiVersion: v1
kind: Service
metadata:
  name: {{ include "securebank.fullname" . }}
  labels:
    {{- include "securebank.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "securebank.selectorLabels" . | nindent 4 }}
