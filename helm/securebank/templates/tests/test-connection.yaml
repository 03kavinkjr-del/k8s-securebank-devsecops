apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "securebank.fullname" . }}-test-connection"
  labels:
    {{- include "securebank.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "securebank.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
